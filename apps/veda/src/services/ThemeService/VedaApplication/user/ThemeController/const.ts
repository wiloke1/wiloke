import configureApp from 'configureApp';

export const baseUrl = `${configureApp.endpoint.product}/${configureApp.endpoint.themes}/publish`;

export const clientBaseUrl = `${configureApp.endpoint.clients}/me/${configureApp.endpoint.themes}`;
