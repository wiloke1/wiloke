import configureApp from 'configureApp';

export const baseUrl = `${configureApp.endpoint.product}/${configureApp.endpoint.themes}/admin`;

export const clientBaseUrl = `${configureApp.endpoint.clients}/admin/${configureApp.endpoint.themes}`;
