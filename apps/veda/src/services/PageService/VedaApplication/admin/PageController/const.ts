import configureApp from 'configureApp';

export const baseUrl = `${configureApp.endpoint.product}/${configureApp.endpoint.pages}/admin`;

export const clientBaseUrl = `${configureApp.endpoint.clients}/admin/${configureApp.endpoint.pages}`;
