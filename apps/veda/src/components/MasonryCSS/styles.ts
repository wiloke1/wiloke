import { css } from 'wiloke-react-core';

export const container = css`
  debug: MasonryCSS_container;
  overflow: hidden;
`;

export const column = css`
  debug: MasonryCSS_column;
  width: 100%;
  display: inline-block;
`;
