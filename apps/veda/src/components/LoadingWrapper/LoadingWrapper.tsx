import { FC } from 'react';
import { View } from 'wiloke-react-core';

export interface LoadingWrapperProps {
  loading?: boolean;
}

const LoadingWrapper: FC<LoadingWrapperProps> = ({}) => {
  return <View>LoadingWrapper</View>;
};

export default LoadingWrapper;
